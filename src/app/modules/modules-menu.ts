import {NbMenuItem} from '@nebular/theme';
import {NbIconConfigUtils} from "@app/core";

export const ALL_MODULES_MENU: NbMenuItem[] = [
    {
      title: 'Bảng điều khiển',
      icon: 'pie-chart',
      link: '/dashboard',
    },
  ]
;
